{% extends 'base.html.twig' %}

{% block title %}Hello PageController!{% endblock %}

{% block body %}
    <h1>{{ post.title }}</h1>

    {{ post.content }}

    <p>
        <strong>{{ post.category ? post.category.name : 'Sin categoría' }}</strong>


        -
        {{ post.comments|length }} Comentarios
    </p>

    <div>
    {% for comment in post.comments %}
        <p>{{ comment.content }}</p>

        <hr>
    {% endfor %}
    </div>
{% endblock %}